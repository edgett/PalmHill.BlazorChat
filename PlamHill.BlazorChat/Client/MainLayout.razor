@using Microsoft.FluentUI.AspNetCore.Components.DesignTokens
@inherits LayoutComponentBase
@inject BaseLayerLuminance BaseLayerLuminance
@inject AccentBaseColor AccentBaseColor

<FluentLayout Class="overflow-block">
    <FluentHeader Class="chat-header" Style="background-color: #FF69B4;">
        Blazor Chat
        <FluentSpacer />
        <FluentAnchor Href="/swagger" Target="_blank">API</FluentAnchor>
    </FluentHeader>

    @Body
</FluentLayout>

<FluentToastProvider />
<FluentDialogProvider />
<FluentTooltipProvider />
<FluentMessageBarProvider />

@code {

    public static bool Expanded { get; set; } = true;
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await BaseLayerLuminance.WithDefault((float)0.1);
            await AccentBaseColor.WithDefault("#0000FF".ToSwatch());

            StateHasChanged();
        }
    }

}
